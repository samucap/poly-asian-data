# TODO

    - implement cleaning functions like normalizing dates, timestamps or whatever
    - review retry logic?
    - should look into subgraph indexing schedule? or patterns to avoid running queries? or is that a thing?
    - handle failures, what to retury and add to some retry queue? especially when the subgraph returns that the indexer's not avail


    - implement ./cmd/main.go as entrypoint to all other cmds and make it into cli tool with cobra?
